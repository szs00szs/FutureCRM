<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.future.hist.crm.dao.AnnouncementMapper">
	<sql id="Base_Column_List">
		<!-- WARNING - @mbggenerated This element is automatically generated by 
			MyBatis Generator, do not modify. -->
		id, title, content, issuer_id, issue_date, department_id
	</sql>

	<insert id="addAnnouncement" parameterType="com.future.hist.crm.domain.Announcement">
		insert into
		announcement (id, title, content,
		issuer_id, issue_date, department_id
		)
		values (#{id}, #{title},
		#{content},
		#{issuer.id},
		#{issue_date}, #{department.id}
		)
	</insert>

	<delete id="deleteAnnouncementById" parameterType="java.lang.Long">
		delete from
		announcement
		where id = #{id}
	</delete>

	<delete id="deleteAnnouncementByTitle" parameterType="java.lang.String">
		delete from
		announcement
		where title = #{title}
	</delete>

	<delete id="deleteAnnouncementByIssuer" parameterType="com.future.hist.crm.domain.User">
		delete
		from announcement
		where issuer_id = #{id}
	</delete>

	<update id="updateAnnouncement" parameterType="com.future.hist.crm.domain.Announcement">
		update
		announcement
			set title = #{title},
			content =#{content},
			issuer_id = #{issuer.id},
			issue_date = #{issue_date},
			department_id = #{department.id}
		where id = #{id,jdbcType=INTEGER}
	</update>

	<select id="getAllAnnouncement" resultMap="resultMap">
		select * from announcement
	</select>

	<select id="getAnnouncementById" parameterType="java.lang.Long"
		resultMap="resultMap">
		select
		<include refid="Base_Column_List" />
		from announcement
		where id = #{id}
	</select>

	<select id="getAnnouncementByTitle" parameterType="java.lang.String"
		resultMap="resultMap">
		select
		<include refid="Base_Column_List" />
		from announcement
		where title = #{title}
	</select>
	
	<select id="getAnnouncementByTitleLike_" parameterType="java.lang.String"
		resultMap="resultMap">
		select 	
		<include refid="Base_Column_List" />
		from announcement
		<where> 
		    <if test="#{_parameter} != null">
		         title like CONCAT('%', #{title}, '%')
		    </if> 
		</where>
	</select>

	<select id="getAnnouncementByIssuer" parameterType="com.future.hist.crm.domain.User"
		resultMap="resultMap">
		select
		<include refid="Base_Column_List" />
		from announcement
		where issuer_id = #{id}
	</select>
	
	<select id="getAnnouncementByDepartment" parameterType="com.future.hist.crm.domain.Department" 
		resultMap="resultMap">
		select
		<include refid="Base_Column_List"/>
		from announcement
		where department_id = #{id}
	</select>
	
	<resultMap type="com.future.hist.crm.domain.Announcement" id="resultMap">
		<id property="id" column="id" />
		<result property="title" column="title" />
		<result property="content" column="content"  />
		<result property="issue_date" column="issue_date"  />
		<association property="issuer" column="issuer_id" select="getUser" />
		<association property="department" column="department_id" select="getDepartment" />
	</resultMap>

	<select id="getUser" parameterType="java.lang.Long"
		resultType="com.future.hist.crm.domain.User">
		select * from sysuser where id = #{id}
	</select>

	<select id="getDepartment" parameterType="java.lang.Long"
		resultType="com.future.hist.crm.domain.Department">
		select * from department where id = #{id}
	</select>
</mapper>